fix a few bugs for me.

1. even if a model is downloaded, it still says to download, however, takes barely 1 second to install it. (maybe cached but not recognized).
2. streaming is not working. it returns 1/2 words, then until full response comes, it doesn't stream from then.
3. remove the live token count from header and put it with the ai message. it's not showing the message properly.
4. use https://github.com/gorhom/react-native-bottom-sheet , but keep the bottom sheet style as is. also, make sure the contents can be scrollable if too much are inside it.
5. for streamdown, can you increase the fontsize and font weight of the latex?
