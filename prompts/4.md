before this point, this app was created using only one model in mind. but now it needs to evolve and use many more models, give users freedom to choose, update, delete models themselves. so i need you to first create create a planned prompt to do this, list the ui requirements, extra modules (if needed) and plan everything as user would love. then, execute the prompt yourself to see those changes.

this update will be on model managements. research all you need from cactus compute for general models, list out their compatibility and make sure to show the users those values for better selection of models. i encourage you to visit https://github.com/cactus-compute/cactus 's readme project. make sure to aggregate providers, for example: gemma, qwen, ... and inside them, their models list.
